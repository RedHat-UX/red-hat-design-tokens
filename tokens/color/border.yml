color:
  border:
    $extensions:
      com.redhat.ux:
        category: border
        collapse: true
        order: 80
        heading: Color

    strong:
      _:
        $value:
          - '{color.border.strong.on-light}'
          - '{color.border.strong.on-dark}'
        $description: >-
          Responsive `border-strong` color value. Typically read only - use a themable container e.g. `<rh-surface>`
          Resolves to `--rh-color-border-strong-on-light` on a themable container with a light color palette
          and `--rh-color-border-strong-on-dark` on a themable container with a dark color palette
      on-light:
        $value: '{color.gray.95}'
        $description: Strong border color (light theme)
        attributes:
          category: border
          type: color
      on-dark:
        $value: '{color.white}'
        $description: Strong border color (dark theme)
        attributes:
          category: border
          type: color

    subtle:
      _:
        $value:
          - '{color.border.subtle.on-light}'
          - '{color.border.subtle.on-dark}'
        $description: >-
          Responsive `border-subtle` color value. Typically read only - use a themable container e.g. `<rh-surface>`
          Resolves to `--rh-color-border-subtle-on-light` on a themable container with a light color palette
          and `--rh-color-border-subtle-on-dark` on a themable container with a dark color palette.
      on-light:
        $value: '{color.gray.30}'
        $description: Subtle border color (light theme)
        attributes:
          category: border
          type: color
      on-dark:
        $value: '{color.gray.50}'
        $description: Subtle border color (dark theme)
        attributes:
          category: border
          type: color

    interactive:
      _:
        $value:
          - '{color.border.interactive.on-light}'
          - '{color.border.interactive.on-dark}'
        $description: >-
          Responsive `border-interactive` color value. Typically read only - use a themable container e.g. `<rh-surface>`
          Resolves to `--rh-color-border-interactive-on-light` on a themable container with a light color palette
          and `--rh-color-border-interactive-on-dark` on a themable container with a dark color palette.
      on-light:
        $value: '{color.blue.50}'
        $description: Interactive border color (light theme)
        attributes:
          category: border
          type: color
      on-dark:
        $value: '{color.blue.30}'
        $description: Interactive border color (dark theme)
        attributes:
          category: border
          type: color

    status:
      danger:
        _:
          $value:
            - '{color.border.status.danger.on-light}'
            - '{color.border.status.danger.on-dark}'
          $description: >-
            Responsive `border-status-danger` color value. Typically read only - use a themable container e.g. `<rh-surface>`
            Resolves to `--rh-color-border-status-danger-on-light` on a themable container with a light color palette
            and `--rh-color-border-status-danger-on-dark` on a themable container with a dark color palette.
        on-light:
          $value: '{color.red-orange.50}'
          $description: Danger status border color (light theme)
          attributes:
            category: border
            type: color
        on-dark:
          $value: '{color.red-orange.40}'
          $description: Danger status border color (dark theme)
          attributes:
            category: border
            type: color
      warning:
        _:
          $value:
            - '{color.border.status.warning.on-light}'
            - '{color.border.status.warning.on-dark}'
          $description: >-
            Responsive `border-status-warning` color value. Typically read only - use a themable container e.g. `<rh-surface>`
            Resolves to `--rh-color-border-status-warning-on-light` on a themable container with a light color palette
            and `--rh-color-border-status-warning-on-dark` on a themable container with a dark color palette.
        on-light:
          $value: '{color.orange.50}'
          $description: Warning status border color (light theme)
          attributes:
            category: border
            type: color
        on-dark:
          $value: '{color.orange.40}'
          $description: Warning status border color (dark theme)
          attributes:
            category: border
            type: color
      default:
        _:
          $value:
            - '{color.border.status.default.on-light}'
            - '{color.border.status.default.on-dark}'
          $description: >-
            Responsive `border-status-default` color value. Typically read only - use a themable container e.g. `<rh-surface>`
            Resolves to `--rh-color-border-status-default-on-light` on a themable container with a light color palette
            and `--rh-color-border-status-default-on-dark` on a themable container with a dark color palette.
        on-light:
          $value: '{color.gray.60}'
          $description: Default status border color (light theme)
          attributes:
            category: border
            type: color
        on-dark:
          $value: '{color.gray.40}'
          $description: Default status border color (dark theme)
          attributes:
            category: border
            type: color
      info:
        _:
          $value:
            - '{color.border.status.info.on-light}'
            - '{color.border.status.info.on-dark}'
          $description: >-
            Responsive `border-status-info` color value. Typically read only - use a themable container e.g. `<rh-surface>`
            Resolves to `--rh-color-border-status-info-on-light` on a themable container with a light color palette
            and `--rh-color-border-status-info-on-dark` on a themable container with a dark color palette.
        on-light:
          $value: '{color.purple.50}'
          $description: Info status border color (light theme)
          attributes:
            category: border
            type: color
        on-dark:
          $value: '{color.purple.30}'
          $description: Info status border color (dark theme)
          attributes:
            category: border
            type: color
      success:
        _:
          $value:
            - '{color.border.status.success.on-light}'
            - '{color.border.status.success.on-dark}'
          $description: >-
            Responsive `border-status-success` color value. Typically read only - use a themable container e.g. `<rh-surface>`
            Resolves to `--rh-color-border-status-success-on-light` on a themable container with a light color palette
            and `--rh-color-border-status-success-on-dark` on a themable container with a dark color palette.
        on-light:
          $value: '{color.green.60}'
          $description: Success status border color (light theme)
          attributes:
            category: border
            type: color
        on-dark:
          $value: '{color.green.40}'
          $description: Success status border color (dark theme)
          attributes:
            category: border
            type: color
