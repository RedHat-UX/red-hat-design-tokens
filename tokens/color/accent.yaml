color:
  accent:
    $extensions:
      com.redhat.ux:
        order: 10
        collapse: true

    brand:
      on-light:
        $value: '{color.red.50}'
        $description: Light theme border accent color (e.g. Accordion, Tabs, etc.)
      on-dark:
        $value: '{color.red.50}'
        $description: Dark theme border accent color (e.g. Accordion, Tabs, etc.)
